/*
import java.util.Comparator;
import java.util.PriorityQueue;
import java.util.Scanner;

public class CountEachAlpabetIinaFile {
  public static void main(String arg[]) {

    PriorityQueue<Integer> pr = new PriorityQueue<Integer>(10, new Comparator<Integer>() {
      @Override
      public int compare(Integer a, Integer b) {
        return b - a;
      }
    });
    for (int i : nums) {
      pr.add(i);
    }
    int i = 0;
    while (i < k - 1) {
      pr.poll();
      i++;
    }
    return pr.poll();

  }
}
*/
